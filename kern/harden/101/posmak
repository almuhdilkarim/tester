
function raptor_kerns_post_conf() {
    
    if [[ $dualboot == true ]];then
        echo "prepare dual boot kernel preset"
        mv /mnt/etc/mkinitcpio.d/linux-hardened-dual.preset /mnt/etc/mkinitcpio.d/linux-hardened.preset
        rm /mnt/etc/mkinitcpio.d/linux-hardened-mono.preset
    else
        echo "prepare single boot kernel preset"
        mv /mnt/etc/mkinitcpio.d/linux-hardened-mono.preset /mnt/etc/mkinitcpio.d/linux-hardened.preset
        rm /mnt/etc/mkinitcpio.d/linux-hardened-dual.preset
    fi
}

function raptor_kerns_post() {
    sleep 2 && clear
    echo "Kernel module configuration"
    raptor_kerns_post_conf
}